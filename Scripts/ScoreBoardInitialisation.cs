using UnityEngine;
using System.Collections;

public class ScoreBoardInitialisation : MonoBehaviour {

	// Use this for initialization
	void Start () {
		gameObject.SetActive (false);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
	public void dada(){
		gameObject.SetActive (false);
	}
}
